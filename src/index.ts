// Este arquivo está de pé apenas como placeholder do build TypeScript.
// A aplicação principal usa a interface web em src/web.ts para captura de câmera + OCR.

console.log("Scanner de documento no modo câmera (sem banco simulado)");
